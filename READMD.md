# flomo2md

Parse flomo export html files to markdown files.

## Usage

Put the html files in a dir named `html`, then run the command below.

```bash
pnpm i
pnpm start
```

For details please refer the simple source code.
